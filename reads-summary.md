# Reads (generated from slides.md)

The mapping decisions for the six sample-story beats. Generated from the `beat:` and `read:` frontmatter fields in [slides.md](slides.md), not hand-edited. To change a mapping, edit the slide's frontmatter — this table is regenerated.

| Beat | Primitive | Params | Read |
|---:|---|---|---|
| 1 | `claim-support` | heading + slot body | two-beat: an observation about brand-deck dissonance followed by a one-line punchline that names the consequence (single-item `support[]` bullet would look weird at slide scale) |
| 2 | `claim-support` | heading + `support[]` (2 bullets) | asymmetric counter-claim (dismisses template library in three words, defines shared spine) followed by an embedded comma-list |
| 3 | `compare` | 2 cells, `as: card` | beat literally says "compare the two worlds" and names both with parenthetical descriptions; contrasting worlds benefit from contained treatment |
| 4 | `sequence` | 4 string steps, `direction: ltr` | explicit four-step ordered process; the `→` arrows in the beat ARE the sequence transitions; string steps preserve verbatim phrasing |
| 5 | `claim-support` | heading + slot body | beat invokes a metric frame ("the payoff in one number") but the "number" is qualitative ("most"); "Reclaim it." is an imperative kicker. `metric` would be more rhetorically literal but lossy (drops the "one number" framing) |
| 6 | `claim-support` | heading + slot body | forward plan (harvest + share) plus an em-dashed exception kicker ("never the tokens"); `divider` was considered (the "Next:" framing) but rejected because the beat carries substantive content |

## Wanted (coverage gaps)

None of the six beats hit a missing primitive — no `wanted:` notes recorded.

But: the **usage skew is the interesting signal**. Four of six beats land on `claim-support`. Either Kendrick's prose is structurally heavy on the assertion+kicker pattern (which is a thing about his voice, not the primitive set), or `claim-support`'s boundary is fuzzy and absorbing things `statement` should hold. The two paths to investigate if this matters:

1. **Story shape.** Are these six beats representative, or does Kendrick's writing skew toward assertion+kicker constructions? Run the method on a different real deck and see whether the distribution moves.
2. **Primitive boundary.** When should `statement` (single focal claim, vast margins) win over `claim-support` (assertion + supporting prose/list)? My current heuristic: if the beat has ONE rhetorical move, `statement`; if it has TWO (claim plus elaboration, evidence, or kicker), `claim-support`. Beats 1, 5, and 6 all have a primary claim plus a kicker sentence — that's two rhetorical moves under my heuristic. The primitive distinction may be working as designed.
